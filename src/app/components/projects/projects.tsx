import Titles from "../ui/titles";
import { navItems } from "../../data/nav";

const Projects = () => {
  return (
    <div className="content pt-14">
      <Titles object={navItems[0]} />
    </div>
  );
};

export default Projects;
